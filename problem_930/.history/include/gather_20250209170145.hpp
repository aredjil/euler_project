#ifndef GATHER_H
#define GATHER_H
#include <iostream>
#include <random>
#include <vector>
#include <algorithm>
#include <string>
#include "gather.hpp"
// Ball 
typedef struct {
    int id;
    int loc;
} ball;
// Bowl 
typedef struct{
    int id; 
    int size;
} bowl;
 

#endif // GATHER_H